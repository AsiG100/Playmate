<div class="card text-center cardEvent">
    <div class="card-header">
        <%=events[event].agesRange %> -
            <%= events[event].level %>
    </div>
    <div class="card-body">
        <h5 class="card-title">
            <%= events[event].name %>
        </h5>
        <p class="card-text">The event is in
            <%= events[event].location %> on
                <%= events[event].dateOfEvent %>
        </p>
        <p class="card-text">Min number of participants is
            <%=events[event].minNumOfParticipants%> and max number of participants is
                <%= events[event].maxNumOfParticipants%>
        </p>
        <form action="/events/<%= events[event]._id %>?_method=DELETE" method="POST" class="dis-inline">
            <input type="submit" class="btn btn-danger" value="Delete">
        </form>
        <a href="/events/<%= events[event]._id %>/edit" class="btn btn-warning">Edit</a>
        <a href="/events/<%= events[event]._id %>" class="btn btn-primary">Go to event</a>
    </div>
    <div class="card-footer text-muted">
        From 
        <%= events[event].timeOfActivity.startTime %>
        to 
        <%= events[event].timeOfActivity.endTime %>
    </div>
</div>
